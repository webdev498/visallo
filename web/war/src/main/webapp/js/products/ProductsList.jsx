define([
    'react'
], function(React) {
    'use strict';

    const PropTypes = React.PropTypes;
    const ProductsList = React.createClass({
        render() {
            return (
                <h1>Title</h1>
            )
        }
    });

    return ProductsList;
});
